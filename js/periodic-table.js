const DATA_URL="data/PubChemElements_all.csv";
const cards=[...document.querySelectorAll(".element")];
function parseCSV(text){const rows=[];let row=[],field="",quoted=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(c==='"'&&quoted&&n==='"'){field+='"';i++;continue}if(c==='"'){quoted=!quoted;continue}if(c===','&&!quoted){row.push(field);field="";continue}if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&n==='\n')i++;row.push(field);if(row.some(v=>v!==''))rows.push(row);row=[];field="";continue}field+=c}if(field!==''||row.length){row.push(field);rows.push(row)}const headers=rows.shift();return rows.map(values=>Object.fromEntries(headers.map((key,i)=>[key,values[i]??""])))}
function fillCard(card,element){card.innerHTML=`<span class="element-number">${element.AtomicNumber}</span><span class="element-symbol">${element.Symbol}</span><span class="element-name-fa" lang="fa">${element.PersianName||"—"}</span><span class="element-name-en" lang="en">${element.Name}</span>`;card.setAttribute("aria-label",`${element.PersianName||element.Name}، ${element.Name}`);card.title=`${element.PersianName||element.Name} — ${element.Name}`}
function blink(card){card.classList.remove("is-blinking");void card.offsetWidth;card.classList.add("is-blinking")}
async function load(){const response=await fetch(DATA_URL,{cache:"no-store"});if(!response.ok)throw new Error(`Unable to load ${DATA_URL}`);const data=parseCSV(await response.text()),elements=new Map(data.map(e=>[String(e.AtomicNumber),e]));cards.forEach(card=>{const element=elements.get(card.dataset.atomicNumber);if(!element)return;fillCard(card,element);card.addEventListener("click",()=>blink(card))});if(elements.size!==118)console.warn(`Expected 118 elements, found ${elements.size}.`)}
load().catch(console.error);